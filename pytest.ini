[pytest]
testpaths = tests
pythonpath = .

# Handlers and scheduler callbacks are coroutines under python-telegram-bot v20+,
# so their tests are async. auto mode runs every `async def test_` without
# needing a marker on each one.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
