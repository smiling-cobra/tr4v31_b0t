# CONTEXT.md

## Purpose

AnxietyJournal is a private Telegram chatbot that helps users externalize anxiety through daily mood check-ins, reflective journaling, supportive responses, and pattern summaries over time.

This file defines shared domain language and behavior-level rules for engineering work.
It is the source of truth for product concepts and boundaries, not coding style or local setup.

## Product Scope

In scope:

- A disclosure, shown before onboarding and repeatable on demand, of how entry text is stored and processed
- First-time onboarding through name, timezone, and reminder-time setup
- Daily check-ins with a mood score and free-text journal entry
- Empathetic LLM responses to journal entries
- Automatic tag extraction from entries
- Recent history, streak stats, and weekly pattern summaries
- Timezone-aware daily reminders and scheduled weekly insights
- Optional low-mood coping guidance and crisis-resource escalation text

Out of scope:

- Diagnosis, therapy, or medical advice
- Crisis intervention beyond surfacing resource text when configured by product logic
- Deployment, infrastructure, and local development setup details
- Coding style, linting, or repository workflow conventions

## Ubiquitous Language

- User: A Telegram user identified by `telegram_id`.
- Onboarding: The first-run setup flow that collects a name, timezone, and daily reminder time.
- Timezone: The user's IANA timezone, captured either from a shared location or typed manually.
- Reminder Time: A daily HH:MM 24-hour time string used for scheduled reminders.
- Check-In: The primary daily interaction where a user rates their mood and writes a journal entry.
- Mood Score: An integer from 1 to 10 representing how the user feels at check-in time.
- Journal Entry: A saved record containing user text, mood score, extracted tags, and creation time.
- Tags: Short themes or triggers extracted from an entry to support pattern recognition.
- Local Day: The calendar day in the user's own timezone. Every day-based rule — streaks, weekly windows, date labels — is evaluated against this, never against the UTC day.
- Streak: The count of consecutive local days with at least one check-in.
- Weekly Summary: A reflection surface that combines the week's mood trend, top tags, and a short LLM-written synthesis.
- Guidance Offer: The follow-up prompt shown after a low-mood check-in, asking whether the user wants concrete coping suggestions.
- Psychological Guidance: Evidence-based, non-clinical coping suggestions generated for low-mood entries.
- Crisis Resources: Static escalation text sent by application logic as its own message at very low mood; it is not generated by the LLM.

## Core Domain Relationships

- A user owns onboarding preferences, including name, timezone, reminder time, and reminder-delivery metadata.
- A journal entry belongs to one user and stores mood score, free-text content, extracted tags, and creation time.
- A streak record belongs to one user and stores the current streak count plus the last check-in timestamp.
- Weekly summaries are derived from a user's recent journal entries rather than stored as a primary record.
- Reminder and weekly-summary delivery state is persisted on the user record to suppress duplicate sends.
- Repositories are persistence adapters around MongoDB collections, while business rules live in services.

## Core User Workflows

1. Onboarding workflow

- A new user starts the bot and is shown what happens to what they write — storage, the third-party model call, the absence of end-to-end encryption, and the limits of the product — before being asked for anything.
- The same notice stays reachable afterwards through a dedicated command.
- A new user is then asked for their name.
- The bot collects a timezone from a shared location or a manually entered IANA timezone.
- The bot collects a daily reminder time in HH:MM 24-hour format.
- The user is marked onboarded and returned to the main menu.

2. Daily check-in workflow

- The user starts a check-in from the main menu.
- The bot collects a mood score from 1 to 10.
- The bot collects a free-text journal entry.
- The system extracts tags, saves the entry, updates streak state, and returns an empathetic LLM response.
- If the mood score is 4 or lower, the bot offers optional coping guidance.

3. Guidance workflow

- The guidance offer appears only after a low-mood check-in.
- If the user accepts, the bot generates concrete coping suggestions tailored to the entry.
- If the user declines, the session returns to the main menu without additional guidance.
- Crisis resources are not part of this workflow. They are delivered during the check-in itself, before the offer is made.

4. Crisis-resource workflow

- When a check-in scores 2 or lower, crisis resources are sent immediately, before the entry is saved or any model is called.
- Delivery is never conditional on an opt-in, on a successful save, or on a successful model response.
- If the mood score is missing when it is needed, the system treats the situation as acute rather than assuming a middling score.
- Crisis resources are also carried in the help text, which stays reachable at any point in a conversation.

5. Reflection workflow

- The user can review recent history of saved entries.
- The user can view current streak, total entries, and average mood.
- The user can view a weekly summary with mood trend rows and top recurring tags.
- An LLM-written weekly synthesis is included only when the week has enough entries to support one.

6. Scheduler workflow

- A background job evaluates onboarded users on a repeating interval.
- Daily reminders are sent in the user's timezone when the stored reminder time is due.
- The system suppresses duplicate reminders on the same day.
- Weekly summaries are sent no more often than every 7 days and only when the user has enough recent entries.

## Bounded Contexts and Code Ownership

- Bot interaction context: Telegram commands, conversation states, message routing, and reply keyboards.
- Journaling context: Entry creation, recent-history retrieval, weekly windows, stats aggregation, and streak semantics.
- Support intelligence context: Empathetic replies, tag extraction, weekly synthesis, and low-mood coping guidance.
- Scheduling context: Due-time calculation, reminder dispatch, and weekly-summary delivery rules.
- Persistence context: MongoDB collection access for users, entries, streaks, and delivery metadata, plus the stored conversation state the bot framework restores after a restart.

Cross-context changes should preserve the domain vocabulary in this file.

## State and Data Rules

- Users are keyed by Telegram identity rather than an internal username.
- Reminder times must be stored as valid HH:MM 24-hour strings.
- Timezones must resolve to valid IANA timezone identifiers before onboarding completes.
- Recent history is a reverse-chronological view of saved entries.
- Entry and streak timestamps are stored as UTC instants. Timezone is a property of how a stored instant is read, not of how it is written.
- Weekly summary data is built from entries within the last 7 local days, today included, starting at local midnight rather than 168 hours back.
- Dates shown to a user are rendered in that user's timezone, so a label never disagrees with the day they lived through.
- Average mood is derived from saved entries and rounded for display.
- Conversation position and a narrow slice of session data — the user's name and the mood score of a check-in in progress — survive a restart or a deploy.
- Raw journal text is never written outside the entries collection. It is deliberately excluded from persisted session data, which is allowlisted rather than filtered, so a newly added field is stored only when someone decides it should be.

## Domain Invariants

- Mood scores must remain in the 1 to 10 range.
- Users must complete onboarding before normal main-menu use.
- The first recorded check-in starts a streak at 1.
- Multiple check-ins on the same local day do not increment a streak more than once.
- A next-local-day check-in increments the streak by 1.
- A gap of more than one local day resets the streak to 1.
- An absent or malformed timezone degrades day boundaries to UTC rather than rejecting the check-in. A streak is not a safety decision, so it fails open; the scheduler deliberately does not share this policy, because a guessed timezone there would message someone in the middle of their night.
- History, stats, reminders, and weekly summaries are user-scoped.
- Weekly narrative synthesis is only generated when there are enough recent entries.
- Crisis hotline text is supplied by application logic, not by the LLM prompt.
- Crisis-resource delivery must not depend on an opt-in, a database write, or a model response.
- Safety decisions must fail closed. Where a mood score is unknown, the system re-asks or assumes the acute case; it never assumes a middling default.

## Non-Functional Domain Constraints

- The product tone must remain private, supportive, and non-clinical.
- LLM failures must degrade gracefully to a safe fallback rather than break the user flow.
- Reminder delivery must remain timezone-aware for each onboarded user.
- Invalid reminder strings or malformed timezones must fail safely rather than trigger sends.
- Business rules should stay concentrated in services, with repositories remaining thin persistence adapters.
- A slow service call must not freeze the runtime. Handlers and the scheduler tick share a single event loop, so
  every synchronous service call they make is dispatched to a worker thread rather than awaited inline, leaving the
  loop free to keep polling, fire scheduled jobs, and finish in-flight API calls.
- This is not per-user concurrency. `Application` processes one update at a time by default, so a slow handler still
  delays the next user's update; the worker thread protects the runtime, not the queue. Unrestricted concurrency is
  not the remedy — `ConversationHandler` state is keyed per chat, and interleaving two updates from the same chat
  corrupts the state machine. Per-chat serialised concurrency is deferred, not solved.
- Scheduled delivery depends on an optional framework component. Its absence must fail loudly at startup rather
  than leave a running bot that silently never sends a reminder.
- A failure must never end in silence. An exception escaping a handler still owes the user a reply and a usable
  keyboard, and a message that belongs to no active conversation re-anchors them to the main menu rather than
  being ignored.
- Losing session state is a recoverable condition, not an error. Recovery cannot tell a wiped session from a
  deliberate cancellation or a first-ever message, so it apologises for nothing and simply restores a usable
  starting point.

## Decisions and Documentation Drift

- CONTEXT.md should describe verified product behavior, not roadmap aspirations.
- When code, tests, and higher-level docs disagree, code and tests are the source of truth.
- Existing roadmap notes in CLAUDE.md understate the currently implemented scheduler and insights behavior.

## How to Update This File

Update CONTEXT.md when any of the following changes:

- New product concepts are introduced
- Existing concepts are renamed or split
- Core onboarding, check-in, guidance, reminder, or reflection workflows change
- Streak, reminder, or weekly-summary rules change
- Safety boundaries or escalation behavior change
- A new architectural decision changes domain vocabulary or boundaries

Keep entries short, concrete, and product-domain focused.
Do not duplicate setup instructions, environment variables, or implementation-level code tours from other docs.